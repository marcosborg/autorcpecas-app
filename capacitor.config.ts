import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pt.autorcpecas.logistics',
  appName: 'Auto RC Peças',
  webDir: 'www/browser'
};

export default config;
